export default function Title({title, subTitle}){
    return(
        <div className="text-header">
            <h1>{title}</h1>
            <h4 className='Senai'>{subTitle}</h4>
        </div>
    )
}